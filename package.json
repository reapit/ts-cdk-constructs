{
  "name": "@reapit/ts-cdk-constructs",
  "packageManager": "yarn@3.6.3",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "jest",
    "check": "yarn workspaces foreach run check",
    "build": "yarn workspaces foreach run build",
    "lint": "yarn eslint packages",
    "root:lint": "eslint"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.5",
    "aws-sdk-client-mock": "^3.0.0",
    "aws-sdk-client-mock-jest": "^3.0.0",
    "eslint": "^8.49.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
